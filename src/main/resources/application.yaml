tokens:
  access-token-available-validity-period-in-sec: 900
  refresh-token-available-validity-period-in-sec: 2592000

spring:
  application:
    name: right-bite
  profiles:
    active: dev
  docker:
    compose:
      lifecycle-management: start_and_stop
      enabled: true
server:
  port: 8080
  forward-headers-strategy: framework

