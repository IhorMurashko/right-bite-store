#30 min
accessTokenAvailableValidityPeriodInSec: 1800
#30 days
refreshTokenAvailableValidityPeriodInSec: 2592000

spring:
  application:
    name: right-bite
  profiles:
    active: dev
  docker:
    compose:
      lifecycle-management: start_and_stop
      enabled: false #i've turned up it to false to resolve all problem with docker
server:
  port: 8080

